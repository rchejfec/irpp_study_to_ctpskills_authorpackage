# Archived: Gemini-era colour experiment scaffolding (2026-07-15)

Working documents from the colour-inventory prep session (RC + Gemini,
2026-07-15). **Untrusted claims, not evidence** — the work was error-prone
and RC flagged it as not ground truth. Everything worth keeping was mined
into `figure_data/theme/registry.json` + `REGISTRY.md` the same day; the
per-figure colour claims are being re-verified against the actual HTML
during the figure-by-figure token migration.

Contents:
- `*_color_inventory.md` (9) — per-figure colour usage claims
- `semantic_requirements.md` — the raw 59-token taxonomy draft
- `core_tags.md` — its distillation to core tokens
- `experimental_portal_pages.md` — superseded by `dist/theme_lab/README.md`

The interactive tooling that accompanied these lives on in `dist/theme_lab/`.
