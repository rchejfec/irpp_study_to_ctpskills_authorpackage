# Handoff — 2026-07-16 (Theme v2 colour v1 live; pushed to portal)

## State

**Theme v2 is through its structural stages and wearing first real colours.**
Pushed to origin 2026-07-16 — **Cloudflare Pages auto-deploys
`figure_data/dist/` on every push to main**; the website team can test the
new iframes on the live portal.

1. **Migration (2026-07-15):** all colour in the 9 live figures + config
   panels flows through semantic `--color-*` tokens (registry:
   `figure_data/theme/`). Per-figure sweep notes in git history
   (`d131925`..); highlights: B2 got a dynamic OKLCH color-mix sequential
   scale with WCAG text-contrast flip (dark ≤3, white ≥4); F2/I bind D3
   attrs to `var(...)` strings; G2 dividers via color-mix inverse @20%.
2. **Purge (2026-07-15):** theme.css contains zero `--clr-*`/`--irpp-*` —
   body, figure-container, gridlines, axis labels, and tippy theme all
   re-pointed to tokens; the nine per-figure fold-in overrides removed;
   canary block deleted (retired). Archived figures keep working via a
   theme.css copy in the archive folder.
3. **tokens.css is the permanent value layer** (RC ruling): imports
   `primitives.css` (org `--pal-*` 13-stop scales + `--ext-neutral-*`
   ramps, generated by theme_lab/generate_palette.js — do not hand-edit).
4. **Colour v1 (RC, 2026-07-15/16):** primitives applied to all tokens.
   Deliberate deviations from PRIMITIVE_MAP proposals (teal = viable
   family; purple = F2 funnel step; susceptible = grey-800; warm/pure
   neutral stops used as plain colours where they looked right — reach,
   not a warm/cool system). Exports rebuilt 2026-07-16 00:59, 9/9.
5. **A3 sidebar hover (2026-07-16):** new `--color-text-brand-hover`
   (navy→teal, explicit semantic-state token) — highlighted rows flip
   sector/key text to teal alongside the badge.

## Next task: colour v2 — "go fishing" (RC direction, 2026-07-16)

Hunt a proper **green family** (status-success is placeholder sage-700,
marked `/* GAP */` in tokens.css), a **more vibrant in-theme purple**,
**polish the greys**, review choices figure by figure. Venue:
`dist/theme_lab/` (colorspace tester). Test list: PRIMITIVE_MAP.md
§ compare-in-place.

## Deferred until colours are final (RC rulings 2026-07-16)

- **F2 legend copy** names stale colours ("Orange" → dots now purple;
  "Light-green" → now *dark* teal). Fix wording only after colour v2.
- **Doc revision pass:** COLOUR_BRIEF.md + PRIMITIVE_MAP.md record
  *proposals*, not the applied state — deliberate (proposals → play →
  revise). Do NOT "correct" figures back toward the docs; the figures
  are ahead. Revise docs when colours stabilize.
- Tooltip copy revision (all figures; D2 Qual. header tooltip still names
  old colours/eras), E2 tooltips, compact on-device verification —
  separate workstreams.

## Trust state

- New 2026-07-16 coverage row (post-purge, primitives live) awaiting RC
  signature; the 2026-07-15 migration row is superseded (its claim cited
  the since-deleted canary lockstep).
- 21 earlier rows signed; row 33's missing date still outstanding (RC).
- `dist/exports/*.png` (2026-07-16 00:59) are the current reference set —
  produced and eyeballed by RC during colour v1; formal sign-off rides
  with colour v2.

## Unreconciled work (2026-07-13, carried forward)

Assessment-notes planning files committed as WIP in `ebb9ab6`
(`assessment_notes_matrix.md`, `assessment_notes_content.md`,
`curated_rationales_scenarios.md`, `data/reference/community_tooltips.json`,
`archive/NOTES_TRACKER.md`). Still unreconciled and unvalidated.

## Watch out

- **Every push to main deploys live** (Cloudflare Pages auto-refresh).
- tokens.css is the ONLY place colour values change; primitives.css is
  generated — regenerate via theme_lab, never hand-edit.
- **Tippy appendTo**: custom tooltips use `appendTo: document.body` to
  avoid iframe clipping — keep it when styling tooltips.
- **B2 dual mode**: `navigator.webdriver` (or `?print`) switches B2's
  layout; Puppeteer always sees the print variant. Web default ≠ export
  PNG by design (DECISIONS § Figure B2).
- Figures fetch JSON — serve `figure_data/dist/` over HTTP
  (`python3 -m http.server 8090` from `dist/`).
